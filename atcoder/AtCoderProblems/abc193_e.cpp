#include <bits/stdc++.h>
using namespace std;

pair<long long, long long> crt(t1, t2, m1, m2) {
  long long r1 = (t1 % m1 + m1) % m1;
}

int main() {
  int t;
  long long x, y, p, q, t1, t2, m1, m2;
  const long long sup = 1LL << 61;
  cin >> t;
  long long ans = sup;
  while (t--) {
    cin >> x >> y >> p >> q;
    m1 = 2 * (x + y);
    m2 = p + q;
    for (long long i = 0; i < q; ++i) {
    }
  }
}
